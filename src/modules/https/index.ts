import fetch from 'node-fetch'

/* // todo -> ponder api designs while I sleep 😴
import { auth } from '../auth/index.js'
import { createRequestOptions } from './request-options.js'
const { appPort: port, ...credentials } = await auth()
const requestOptions = createRequestOptions(credentials)
const endpoint = `https://127.0.0.1:${port}/lol-summoner/v1/current-summoner`
const method = 'GET'
await fetch(endpoint, {
  method,
  ...requestOptions
})
  .then((response) => response.json())
  .then(console.log)
*/
